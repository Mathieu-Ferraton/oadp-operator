apiVersion: oadp.openshift.io/v1alpha1
kind: Velero
metadata:
  name: velero-sample
spec:
  # Add fields here
  olmManaged: false
  backupStorageLocations:
  - provider: aws
    default: true
    objectStorage:
      bucket: my-minio-bucket
      prefix: 'velero'
    config:
      region: minio
      profile: "default"
      s3ForcePathStyle: 'true'
      s3Url: "http://9.30.250.138:9000"
    credential:
      name: cloud-credentials
      key: cloud
  enable_restic: true
  defaultVeleroPlugins:
  - openshift
  - aws
